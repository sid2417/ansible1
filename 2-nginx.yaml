- name: Installing Nginx playbook
  hosts: localhost
  become: yes
  tasks:
  - name: Installing Nginx
    ansible.builtin.dnf:
      name: nginx
      state: latest
  - name: starting Nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes